data:
  data_path: [./data.csv]
  image_height: 512
  image_width: 512
  transform_name: resize_crop
  type: VideoTextDataset
  num_frames: 17
  frame_interval: 1
# model config
model:
  model: Mobiledit_300M_P1_D16
  image_height: 512
  image_width: 512
  mixed_precision: fp16
  fp32_attention: true
  #load_from: ./model/hybrid_371.pth
  load_from: ./model/hybrid_371.pth
  teacher_model_path: ./model/hybrid_371.pth
  dmd_model_path: ./model/hybrid_371.pth
  # resume_from:
  aspect_ratio_type: ASPECT_RATIO_512
  multi_scale: false
  attn_type: linear
  linear_head_dim: 32
  ffn_type: glumbconv
  mlp_acts:
    - silu
    - silu
    - null
  mlp_ratio: 2.5
  use_pe: false
  qk_norm: true
  class_dropout_prob: 0.1
  cfg_scale: 5
  cfg_embed: true
  cfg_embed_scale: 0.1
# VAE setting
vae:
  vae_type: video-vae
  vae_pretrained: ./model/video-vae
  scale_factor: 0.41407
  vae_latent_dim: 128
  vae_downsample_rate: 32
  sample_posterior: true
# text encoder
text_encoder:
  text_encoder_name: ./model/Qwen2-0.5B
  y_norm: true
  y_norm_scale_factor: 0.01
  model_max_length: 300
  caption_channels: 896
  # CHI
  # chi_prompt:
  #   - 'Given a user prompt, generate an "Enhanced prompt" that provides detailed visual descriptions suitable for image generation. Evaluate the level of detail in the user prompt:'
  #   - '- If the prompt is simple, focus on adding specifics about colors, shapes, sizes, textures, and spatial relationships to create vivid and concrete scenes.'
  #   - '- If the prompt is already detailed, refine and enhance the existing details slightly without overcomplicating.'
  #   - 'Here are examples of how to transform or refine prompts:'
  #   - '- User Prompt: A cat sleeping -> Enhanced: A small, fluffy white cat curled up in a round shape, sleeping peacefully on a warm sunny windowsill, surrounded by pots of blooming red flowers.'
  #   - '- User Prompt: A busy city street -> Enhanced: A bustling city street scene at dusk, featuring glowing street lamps, a diverse crowd of people in colorful clothing, and a double-decker bus passing by towering glass skyscrapers.'
  #   - 'Please generate only the enhanced description for the prompt below and avoid including any additional commentary or evaluations:'
  #   - 'User Prompt: '
# Sana schedule Flow
scheduler:
  train_sampling_steps: 1000
  predict_v: true
  noise_schedule: linear_flow
  pred_sigma: false
  flow_shift: 1.0
  # logit-normal timestep
  weighting_scheme: logit_normal
  logit_mean: 0.0
  logit_std: 1.0
  #vis_sampler: flow_dpm-solver
  vis_sampler: flow_euler
# training setting
train:
  num_workers: 4
  seed: 1
  train_batch_size: 2
  num_epochs: 1000
  gradient_accumulation_steps: 1
  grad_checkpointing: true
  gradient_clip: 1.0
  optimizer:
    betas:
      - 0.9
      - 0.999
      - 0.9999
    eps:
      - 1.0e-30
      - 1.0e-16
    # lr: 0.0001
    lr: 0.0001
    type: CAMEWrapper
    weight_decay: 0.1
  lr_schedule: constant
  lr_schedule_args:
    num_warmup_steps: 2000
  local_save_vis: true # if save log image locally
  visualize: true
  eval_sampling_steps: 3000
  log_interval: 50
  save_model_epochs: 500
  save_model_steps: 2000
  work_dir: output/debug
  online_metric: false
  eval_metric_step: 100000
  online_metric_dir: metric_helper
  validation_prompts:
    - test1
    - test2
    - test3
    - test4
    - test5